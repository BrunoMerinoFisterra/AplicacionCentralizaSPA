import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.fisterra.centraliza',
  appName: 'Centraliza SPA App',
  webDir: 'dist',
};

export default config;
